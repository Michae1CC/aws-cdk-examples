export type AwsAccessKey = {
  AccessKeyId: string;
  SecretAccessKey: string;
};

export type TableItem = {
  description: {
    S: string;
  };
};

export type TableJSObject = {
  description: string;
};
